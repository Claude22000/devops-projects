name: Test GKE ARC Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: standard
    steps:
      - name: Check runner
        run: |
          echo "Running inside GKE ARC runner"
          uname -a
          docker version
